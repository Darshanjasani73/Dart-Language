void main() {
  print(sum(4, 5));
  Show(4, 7);
}

int sum(a,  b) {
  return a + b;
}

void Show(int a, int b) {
  // int a or a both is valid.
  print(a + b);
}
/*
function is assume the dynamic or parameters are assume the dynamic if don't declare any datatype.
*/
